<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark">

    <io.github.mattpvaughn.chronicle.views.BottomSheetChooser
        android:id="@+id/bottom_sheet_chooser"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="1dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout="@layout/view_bottom_sheet_chooser" />

    <!--
      The rows are `SettingsScreen` now (cu-199). `SettingsList` — a FrameLayout wrapping a
      programmatic RecyclerView, three ViewHolders and a DiffUtil — is deleted with it.

      `BottomSheetChooser` above stays a View: it is shared, animated, and its state carries a
      listener object. Same call cu-187 made for SwipeRefreshLayout and cu-198 for the chapter
      list. It keeps its elevation so it still draws over the rows.
    -->
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/settings_compose"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
