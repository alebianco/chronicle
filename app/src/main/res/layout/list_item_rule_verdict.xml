<?xml version="1.0" encoding="utf-8"?>
<!--
  One rule's verdict in the tester (cu-151).

  The verdict is carried by **text**, never by colour alone — a green tick and a grey one are the
  same row to a colour-blind reader and to a screen reader (cu-47). The icon is decorative for that
  reason and the row's own contentDescription says the whole verdict.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="@dimen/margin_small">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/rule_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/textPrimary"
            android:textSize="14sp"
            tools:text="audnexus" />

        <!-- "Your rule" / "Built in". Always present: which rules are the user's own is half the
             question the screen answers. -->
        <TextView
            android:id="@+id/rule_origin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/textSecondary"
            android:textSize="14sp"
            tools:text="Built in" />

    </LinearLayout>

    <TextView
        android:id="@+id/rule_verdict"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/textSecondary"
        android:textSize="14sp"
        tools:text="Matched, and this is the rule that decided" />

</LinearLayout>
