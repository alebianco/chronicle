<?xml version="1.0" encoding="utf-8"?>
<!--
  Browsing the library by author, narrator or series (cu-24).

  The facet tabs, the value list, the coverage line and the empty message are all `BrowseScreen`
  now (cu-202) — a `TabLayout`, a `RecyclerView` with `FacetListAdapter`, and three `isVisible`
  decisions across two render passes. The toolbar stays a View: it owns the up-navigation contract
  with the FragmentManager, which is not this screen's content.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/browse_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/ic_arrow_back_white"
        app:title="@string/browse_title"
        app:titleTextColor="@color/textPrimary" />

    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/browse_compose"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/browse_toolbar" />

</androidx.constraintlayout.widget.ConstraintLayout>
