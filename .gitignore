# build artifacts
*.dex
*.class

# generated files
build/
tmp/
captures/
bin/
gen/
target/
app/freeAsInBeer
app/googlePlay
app/release

# android studio files
.idea/
*.iml

# local config files
.gradle
local.properties
build.xml

# etc
.externalNativeBuild
.cxx
*.swp
*.un~
.*.un~
*.keystore

# OS Generated Files
*~
._*
.DS_Store
.DS_Store?
.Spotlight-V100
.Trashes
Desktop.ini
ehthumbs.db
Icon?
Thumbs.db

# Superpowers working scratch — never committed.
# plans/ and specs/ are drafting output; their durable content is redirected into
# backlog/ instead (execution plans -> the task's ## Implementation Plan; design
# specs -> a backlog/decisions/ record or the task description). See CLAUDE.md §Workflow.
docs/superpowers/

# Kotlin build tooling session data
.kotlin/

# direnv. .envrc here selects which GitHub account gh uses for this repo, which
# is a property of one person's machine and not of the project.
.envrc
.direnv/

# Git worktrees (owner convention: .worktree/<branch-name>)
.worktree/
# Agent worktrees, created by the harness per subagent. Not ignoring these lets a
# `git add -A` commit them as gitlinks, which is how three landed in a commit once.
.claude/worktrees/

# Captured device session prefs — these hold real Plex tokens. plex-session.sh keeps its
# backup in $HOME by default; these rules exist so a copy made inside the repo by mistake
# cannot be committed.
ChronicleAuth.xml
Chronicle.xml
chronicle_debug.xml
.chronicle-session-backup/

# Machine-local Gradle JDK pin: holds an absolute java.home path, so it cannot be shared.
# Studio reads it via gradleJvm=#GRADLE_LOCAL_JAVA_HOME; the portable half of the pin is
# .tool-versions, which is committed.
gradle/config.properties

# JVM crash dumps. PIT deliberately runs mutants that can kill a minion JVM, so a mutation run
# can leave these behind; they are diagnostics for a crash that is expected, not build output.
hs_err_pid*.log
replay_pid*.log
